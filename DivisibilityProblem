#pragma GCC target("avx2")
 
#include <iostream>
 
int main() 
{
	std::ios::sync_with_stdio(false);
	
	int t;
	std::cin >> t;
	
	for (int i = 0; i < t; ++i)
	{
		int a, b;
		std::cin >> a >> b;
		std::cout << ( !(a % b) ? 0 : b - a % b );
		std::cout << std::endl;
	}
	
	return 0;
}