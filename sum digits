#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,i,val,rem;
    scanf("%d", &n);
    while(n!=0)
    {
      rem=n%10;
      val=val+rem;
      n/=10;
    }
    printf("%d",val);
    return 0;
}
Question:
