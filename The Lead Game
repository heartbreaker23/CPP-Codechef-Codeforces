#include<bits/stdc++.h>
using namespace std;

int main()
{int t, max=0,a,b,c=0,d=0,r;
  cin >> t;
  while (t--)
{ 
cin >> a>>b;
c+=a;
d+=b;
if(c-d>max)
{r=1;
max=c-d;}
if(d-c>max)
{r=2;
max=d-c;
}
}
cout<<r<<" "<<max<<endl;
return 0;
}

Question Link :-https://www.codechef.com/problems/TLG
