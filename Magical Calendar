#include <bits/stdc++.h>

using namespace std;


int main()
{
    int t;
    cin>>t;
    while(t--)
{
long long int n,r;
cin>>n>>r;
long long int a;
if(n<=r)
{
a=(n*(n-1))/2+1;
cout<<a<<endl;
}
else
{
a =(r*(r+1))/2;
cout<<a<<endl;
}

}
return 0;
}
